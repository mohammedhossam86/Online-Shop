<nav class="navbar navbar-expand-lg navbar-dark bg-dark app-navbar shadow-sm">
    <div class="container-fluid">
        <a class="navbar-brand fw-bold" href="/">🛒 Online Shop</a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto gap-2">

                <li class="nav-item">
                    <a class="nav-link active" href="/">Home</a>
                </li>

                <!-- ✅ CUSTOMER -->
                <% if (isUser && role === 'customer') { %>
                    <li class="nav-item"><a class="nav-link" href="/cart">Cart</a></li>
                    <li class="nav-item"><a class="nav-link" href="/orders">My Orders</a></li>
                <% } %>

                <!-- ✅ PROVIDER -->
                <% if (isUser && role === 'provider') { %>
                    <li class="nav-item"><a class="nav-link" href="/products/add">Add Product</a></li>
                    <li class="nav-item"><a class="nav-link" href="/products/provider">My Products</a></li>
                <% } %>

                <!-- ✅ ADMIN -->
                <% if (isUser && role === 'admin') { %>
                    <li class="nav-item"><a class="nav-link" href="/admin/products">Manage Products</a></li>
                    <li class="nav-item"><a class="nav-link" href="/admin/orders">Manage Orders</a></li>
                <% } %>

                <!-- ✅ GUEST -->
                <% if (!isUser) { %>
                    <li class="nav-item"><a class="nav-link" href="/login">Login</a></li>
                    <li class="nav-item"><a class="nav-link" href="/signup">Signup</a></li>
                <% } %>

            </ul>

            <% if (isUser) { %>
                <form action="/logout" method="post">
                    <button class="btn btn-danger btn-sm">Logout</button>
                </form>
            <% } %>
        </div>
    </div>
</nav>